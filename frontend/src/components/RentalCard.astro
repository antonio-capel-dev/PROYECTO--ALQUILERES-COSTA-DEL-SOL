---
import Badge from "./ui/Badge.astro";
import Button from "./ui/Button.astro";
import type { Rental } from "../types/rental";

interface Props extends Rental {}

const { title, price, slug, image, location, servicios, detalles } =
  Astro.props;


const { wifi, piscina, aire_acondicionado, mascotas, parking } =
  servicios || {};
const { habitaciones, banos, metros_cuadrados, capacidad } = detalles || {};

const imageUrl = image?.url;
const locationName = location || "Costa del Sol";
---


<article
  class="group bg-white rounded-2xl shadow-sm hover:shadow-xl transition-all duration-300 border border-slate-100/50 overflow-hidden flex flex-col h-full transform hover:-translate-y-1"
>
  {/* ZONA SUPERIOR: FOTO + PRECIO */}
  <div class="relative h-64 bg-slate-200 overflow-hidden">
    <div
      class="absolute inset-0 flex items-center justify-center text-slate-400 group-hover:scale-105 transition duration-700 ease-out"
    >
      <img
        src={imageUrl && imageUrl !== ""
          ? imageUrl
          : "https://images.unsplash.com/photo-1512917774080-9991f1c4c750?auto=format&fit=crop&w=800&q=80"}
        alt={title}
        class="w-full h-full object-cover"
      />
    </div>

    <!-- PRECIO FLOTANTE -->
    <div
      class="absolute top-4 right-4 bg-white/95 backdrop-blur-sm px-4 py-2 rounded-full shadow-lg border border-slate-100/50"
    >
      <span class="font-bold text-brand-dark text-lg">{price}€</span>
      <span class="text-xs text-slate-500 font-normal">/ noche</span>
    </div>

    <!-- BADGES DE DESTACADOS -->
    <div class="absolute bottom-4 left-4 flex gap-2">
      {
        piscina && (
          <span class="bg-blue-500/90 backdrop-blur-sm text-white px-2 py-1 rounded-md text-xs font-medium shadow-sm">
            🏊 Piscina
          </span>
        )
      }
      {
        wifi && (
          <span class="bg-indigo-500/90 backdrop-blur-sm text-white px-2 py-1 rounded-md text-xs font-medium shadow-sm">
            📶 Wifi
          </span>
        )
      }
    </div>
  </div>

  {/* ZONA INFERIOR: TEXTO + DETALLES + BOTÓN */}
  <div class="p-6 flex flex-col flex-grow">
    <!-- TÍTULO -->
    <h3
      class="text-xl font-bold text-brand-dark group-hover:text-brand-primary transition line-clamp-1 mb-2"
    >
      {title}
    </h3>

    <!-- UBICACIÓN -->
    <div class="flex items-center text-slate-500 text-sm mb-4">
      <span class="mr-1 text-brand-accent">📍</span>
      {locationName}
    </div>

    <!-- DETALLES  - Inspirado en competidor pero estilo propio -->
    <div
      class="grid grid-cols-3 gap-2 py-4 mb-4 border-t border-slate-100 text-sm text-slate-600"
    >
      <div
        class="flex flex-col items-center justify-center p-2 bg-slate-50 rounded-lg"
      >
        <span class="text-lg mb-1">🛏️</span>
        <span>{habitaciones || "-"} Hab.</span>
      </div>
      <div
        class="flex flex-col items-center justify-center p-2 bg-slate-50 rounded-lg"
      >
        <span class="text-lg mb-1">🚿</span>
        <span>{banos || "-"} Baños</span>
      </div>
      <div
        class="flex flex-col items-center justify-center p-2 bg-slate-50 rounded-lg"
      >
        <span class="text-lg mb-1">👥</span>
        <span>{capacidad || "-"} Pers.</span>
      </div>
    </div>

    <!-- BOTÓN ACCIÓN -->
    <Button
      href={`/propiedad/${slug}`}
      variant="outline"
      size="sm"
      class="w-full mt-auto group-hover:bg-brand-primary group-hover:text-white group-hover:border-brand-primary transition-colors"
    >
      Ver Disponibilidad
    </Button>
  </div>
</article>
